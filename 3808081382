--// RoHub - Rocta | TouchPart Spammer (Modern Edition)
-- Credits: RoHub - Rocta

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer

-- Cleanup existing GUI
if game.CoreGui:FindFirstChild("RoHubRocta_TouchSpammer") then
    game.CoreGui.RoHubRocta_TouchSpammer:Destroy()
end

-- Create main ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RoHubRocta_TouchSpammer"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui
ScreenGui.DisplayOrder = 100
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame with modern styling
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 280, 0, 180)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -90)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui
MainFrame.ClipsDescendants = true
MainFrame.Active = true
MainFrame.Selectable = true

-- Rounded corners
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 22)
UICorner.Parent = MainFrame

-- Modern gradient background
local UIGradient = Instance.new("UIGradient")
UIGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 50)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(25, 25, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(35, 25, 45))
}
UIGradient.Rotation = 135
UIGradient.Parent = MainFrame

-- Subtle animated border glow
local BorderGlow = Instance.new("Frame")
BorderGlow.Size = UDim2.new(1, 6, 1, 6)
BorderGlow.Position = UDim2.new(0, -3, 0, -3)
BorderGlow.BackgroundTransparency = 1
BorderGlow.BorderSizePixel = 0
BorderGlow.Parent = MainFrame

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(0, 24)
GlowCorner.Parent = BorderGlow

local GlowGradient = Instance.new("UIGradient")
GlowGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(138, 43, 226)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 212, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(138, 43, 226))
}
GlowGradient.Rotation = 90
GlowGradient.Transparency = NumberSequence.new{
    NumberSequenceKeypoint.new(0, 0.8),
    NumberSequenceKeypoint.new(0.5, 0.4),
    NumberSequenceKeypoint.new(1, 0.8)
}
GlowGradient.Parent = BorderGlow

-- Title with branding (fixed text color for readability)
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, 0, 0, 45)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "RoHub - Rocta"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 20
Title.Font = Enum.Font.GothamBlack
Title.Parent = MainFrame

-- Animated rainbow stroke for title (kept but refined)
local TitleStroke = Instance.new("UIStroke")
TitleStroke.Thickness = 2
TitleStroke.Color = Color3.fromRGB(255, 100, 100)
TitleStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
TitleStroke.Parent = Title

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 22)
TitleCorner.Parent = Title

-- Status indicator
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -20, 0, 20)
StatusLabel.Position = UDim2.new(0, 10, 0, 42)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "● Ready"
StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 140)
StatusLabel.TextSize = 13
StatusLabel.Font = Enum.Font.GothamMedium
StatusLabel.Parent = MainFrame

-- Action Button (modern styling, fixed text color)
local SpamButton = Instance.new("TextButton")
SpamButton.Name = "SpamButton"
SpamButton.Size = UDim2.new(0, 220, 0, 50)
SpamButton.Position = UDim2.new(0.5, -110, 0.5, 10)
SpamButton.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
SpamButton.Text = "Start Spamming"
SpamButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SpamButton.TextSize = 16
SpamButton.Font = Enum.Font.GothamSemibold
SpamButton.Parent = MainFrame
SpamButton.AutoButtonColor = false

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 16)
ButtonCorner.Parent = SpamButton

local ButtonGradient = Instance.new("UIGradient")
ButtonGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(88, 45, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(138, 43, 226))
}
ButtonGradient.Parent = SpamButton

-- Rainbow stroke effect for button (kept but optimized)
local ButtonStroke = Instance.new("UIStroke")
ButtonStroke.Thickness = 2
ButtonStroke.Color = Color3.fromRGB(255, 100, 100)
ButtonStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
ButtonStroke.Parent = SpamButton

-- Close button (top right)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -38, 0, 8)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "✕"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
CloseBtn.TextSize = 18
CloseBtn.Parent = MainFrame

CloseBtn.MouseButton1Click:Connect(function()
    -- Fade out animation
    local TweenService = game:GetService("TweenService")
    TweenService:Create(MainFrame, TweenInfo.new(0.25), {
        Size = UDim2.new(0, 0, 0, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0)
    }):Play()
    task.wait(0.25)
    ScreenGui:Destroy()
end)

-- Hover/press effects for button
SpamButton.MouseEnter:Connect(function()
    local TweenService = game:GetService("TweenService")
    TweenService:Create(SpamButton, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(60, 60, 85)
    }):Play()
end)

SpamButton.MouseLeave:Connect(function()
    local TweenService = game:GetService("TweenService")
    TweenService:Create(SpamButton, TweenInfo.new(0.15), {
        BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    }):Play()
end)

-- Rainbow animation loop (optimized)
spawn(function()
    local hue = 0
    local TweenService = game:GetService("TweenService")
    while ScreenGui.Parent do
        hue = (hue + 4) % 360
        local rainbowColor = Color3.fromHSV(hue / 360, 0.9, 1)
        
        -- Smooth color transition using TweenService
        TweenService:Create(TitleStroke, TweenInfo.new(0.1), {Color = rainbowColor}):Play()
        TweenService:Create(ButtonStroke, TweenInfo.new(0.1), {Color = rainbowColor}):Play()
        
        task.wait(0.016) -- ~60 FPS
    end
end)

-- Core spamming functionality
local isSpamming = false
local spamConnection

local function runTouchScript()
    local character = LocalPlayer.Character
    if not character then return end
    
    local r = character:FindFirstChild("HumanoidRootPart")
    if not r then return end
    
    for _, p in pairs(workspace:GetDescendants()) do
        if p.Name == "TouchPart" and p:IsA("BasePart") then
            pcall(function()
                firetouchinterest(r, p, 0)
                firetouchinterest(r, p, 1)
            end)
        end
    end
end

-- Button click handler
SpamButton.MouseButton1Click:Connect(function()
    isSpamming = not isSpamming
    
    local TweenService = game:GetService("TweenService")
    
    if isSpamming then
        SpamButton.Text = "Stop Spamming"
        StatusLabel.Text = "● Active"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        
        -- Visual feedback animation
        TweenService:Create(SpamButton, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        }):Play()
        
        spamConnection = RunService.Heartbeat:Connect(function()
            pcall(runTouchScript)
        end)
    else
        SpamButton.Text = "Start Spamming"
        StatusLabel.Text = "● Ready"
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 140)
        
        TweenService:Create(SpamButton, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(45, 45, 65)
        }):Play()
        
        if spamConnection then
            spamConnection:Disconnect()
            spamConnection = nil
        end
    end
end)

-- Draggable functionality (mobile + PC optimized)
local dragging = false
local dragStart = nil
local startPos = nil

local function updateDrag(input)
    if not dragging or not dragStart or not startPos then return end
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y
    )
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or 
       input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

MainFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or 
       input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
        dragStart = nil
        startPos = nil
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or 
                     input.UserInputType == Enum.UserInputType.Touch) then
        updateDrag(input)
    end
end)

-- Mobile optimization: ensure proper touch target sizes
if UserInputService.TouchEnabled then
    SpamButton.Size = UDim2.new(0, 240, 0, 55)
    SpamButton.Position = UDim2.new(0.5, -120, 0.5, 10)
    SpamButton.TextSize = 17
end

-- Fade in animation on load
task.spawn(function()
    local TweenService = game:GetService("TweenService")
    MainFrame.BackgroundTransparency = 1
    Title.TextTransparency = 1
    SpamButton.BackgroundTransparency = 1
    SpamButton.TextTransparency = 1
    
    task.wait(0.1)
    
    TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {BackgroundTransparency = 0}):Play()
    TweenService:Create(Title, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {TextTransparency = 0}):Play()
    TweenService:Create(SpamButton, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {BackgroundTransparency = 0, TextTransparency = 0}):Play()
end)

-- Return module table for external integration (optional)
return {
    GUI = ScreenGui,
    ToggleSpam = function(state)
        if state ~= isSpamming then
            SpamButton:FireClick()
        end
    end,
    IsSpamming = function()
        return isSpamming
    end
}
